# Comparison Operator Replacement

Mutation operator: replaces a comparison call head (`==`, `/=`, `<`, `>`,
`=<`, `>=`) with a different comparison operator at each call site. Defined
in [[SPEC-001-lfe-mutation-testing#REQ-002]].
