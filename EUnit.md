# EUnit

Erlang/OTP's built-in unit test framework. alien-mutants runs a project's
existing EUnit suite against each mutant to classify it killed or survived —
see [[SPEC-001-lfe-mutation-testing#REQ-004]].
