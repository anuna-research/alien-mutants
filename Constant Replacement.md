# Constant Replacement

Mutation operator: replaces literal constants (`0` → `1`, `[]` → a non-empty
sentinel, `true`/`false` swap) at each literal site. Defined in
[[SPEC-001-lfe-mutation-testing#REQ-002]].
