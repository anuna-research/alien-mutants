# Arithmetic Operator Replacement

Mutation operator: replaces an arithmetic call head (`+`, `-`, `*`, `div`,
`rem`) with a different arithmetic operator of the same arity at each call
site. Defined in [[SPEC-001-lfe-mutation-testing#REQ-002]].
